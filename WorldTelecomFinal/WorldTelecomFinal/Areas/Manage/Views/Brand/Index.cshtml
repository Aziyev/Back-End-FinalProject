@model List<Brand>

@{
    ViewData["Title"] = "Index";
    int no = 1;
}


<div class="content-wrapper" style="margin-left:50px;">
            <!-- Content -->

            <div class="container-xxl flex-grow-1 container-p-y">
              

              <!-- Basic Bootstrap Table -->
              <div class="card">
                <h5 class="card-header">Brands</h5>

                <div style="text-align:center;" >
                    <form>
                        <div>
                            <label style="font-weight:700; padding-right:10px; font-size:20px;" class="col-form-label" >Brand Status Choose</label>
                            <select name="status" style="padding:5px 60px; background-color:lightseagreen; border-radius:5px; font-weight:700;">
                               
                            @if(ViewBag.Status == 0)
                            {
                                <option selected value="0" class="">All</option>
                            }
                            else
                            {
                                <option  value="0" class="">All</option>
                            }

                            @if(ViewBag.Status == 0)
                            {
                                <option selected value="1" class="">Deleted</option>
                            }
                            else
                            {
                                <option value="1" class="">Deleted</option>
                            }

                            @if(ViewBag.Status == 0)
                            {
                               
                                <option selected value="2" class="">NotDeleted</option>
                            }
                            else
                            {
                                <option value="2" class="">NotDeleted</option>
                            }

                            </select>
                        </div>
                        <button type="submit">Filer</button>
                    </form>
                </div>

                <div style="text-align:end;">
                    <a asp-controller="brand" asp-action="create" asp-area="manage" style="border:1px solid black; padding:5px 10px; margin-right:10px; border-radius:10px; background-color:green; color:white;">
                        Create
                        </a>
                </div>
                <div class="table-responsive text-nowrap tbl-content">
                
                    @await Html.PartialAsync("_BrandIndexPartial",Model)

                </div>
              </div>

              
            </div>
 
            <div class="content-backdrop fade"></div>
          </div>

@*
    <div class="row">
        <div class="col-lg-12">
            <table>
               <thead>
                 <tr>
                    <th>NO</th>
                    <th>Name</th>
                </tr>
               </thead>
               <tbody>
                   @foreach (Brand brand in Model)
                    {
                        <tr>
                            <td>@no</td>
                            <td>@brand.Name</td>
                        </tr>

                        no++;
                    }
               </tbody>
            </table>
        </div>

    </div>*@
   




@*                <div class="row justify-content-between">
    <div class="col-lg-2">
        <h1>Brands</h1>
    </div>
    <div class="col-lg-6">
        <form>
            <div class="row">
                <div class="col">
                    <label class="form-label">Brand Statusu</label>
                    <select name="status" class="form-select" aria-label="Default select example">
                        @if (ViewBag.Status == 0)
                        {
                            <option selected value="0">All</option>
                        }
                        else
                        {
                            <option value="0">All</option>
                        }

                        @if (ViewBag.Status == 1)
                        {
                            <option selected value="1">Silinenler</option>
                        }
                        else
                        {
                            <option value="1">Silinenler</option>
                        }

                        @if (ViewBag.Status == 2)
                        {
                            <option selected value="2">Silinmiyenler</option>
                        }
                        else
                        {
                            <option value="2">Silinmiyenler</option>
                        }
                    </select>
                </div>
                <div class="col">
                    <button class="btn btn-primary" type="submit">Filter</button>
                </div>
            </div>
        </form>
    </div>
    <div class="col-lg-1">
        <a class="btn btn-success" asp-area="manage" asp-controller="brand" asp-action="create">Create</a>
    </div>
</div>
<div class="row">
    <div class="col-lg-12 tblContent">
        @await Html.PartialAsync("_BrandIndexPartial", Model) 
    </div>
</div>*@